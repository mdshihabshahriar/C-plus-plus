#include<bits/stdc++.h>
using namespace std;
#define optimize() ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
int main()
{
    optimize();
    string s = "Shahriar";
    if(s.find("ria") != -1) cout << "Found"; // substring naki eita diye bujha jabe.Jehetu ria ache string er moddhe tai substring and found.
    else cout << "Not found";
    return 0;
}
